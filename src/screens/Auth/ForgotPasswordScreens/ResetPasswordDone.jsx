import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ResetPasswordDone = () => {
  return (
    <View>
      <Text>ResetPasswordDone</Text>
    </View>
  );
};

export default ResetPasswordDone;

const styles = StyleSheet.create({});
